# Server Port
server.port=8080

# Spring Data MongoDB Configuration
# Local MongoDB connection URI (default port is 27017)
spring.data.mongodb.uri=mongodb://localhost:27017/packinglist

# Enable detailed logging for debugging if needed
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.com.packinglist=DEBUG
